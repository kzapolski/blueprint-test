### Very simple blueprint example

##### How to run / test:
Clone it then run
 
```bash
pip install -r requirements.txt

./run.py
```
Then in separate terminal

```
curl localhost:5000/

curl localhost:5000/foo/

curl localhost:5000/foo/bar

```